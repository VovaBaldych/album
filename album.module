<?php

/**
 * Implements hook_theme().
 */
function album_theme($existing, $type, $theme, $path) {
  return [
    'my_template' => [
      'variables' => ['test_var' => NULL,],
    ],

    'album_form' => [
      'render element' => 'form',
    ]
  ];
}